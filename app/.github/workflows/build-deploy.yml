name: Build and deploy code

on: [push, pull_request]


jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: "Pull git repo"
        uses: actions/checkout@v7
      - name: say hello to ryan
        run: echo 'Hello Ryan!!'